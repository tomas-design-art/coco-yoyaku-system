# 指示書5【2026-08-18】交渉ループ実装・幻覚停止・来週バグ・HotPepper未同期618件の掃除

対象実装者: VS Code Copilot / 対象リポ: `Yoyaku_AppV2`
本書は指示書4（LLM-ONLY方針）を引き継ぐ。返信生成の原則は指示書4のまま（**LLMが読み、LLMが書き、そのまま送る**）。

---

## 0. 現状（実機で確認された事実）

指示書4の修正で、**1通目の応答は実用レベルに到達した**。実機ログ:

```
患者: 月曜日にいつもの予約したい
Bot : 時田さん、ご連絡ありがとうございます。8/17(月)のマッスルセラピーですね。
      当日は13:00〜、14:15〜、15:15〜の枠が空いておりますが、ご都合はいかがでしょうか？😊   ← ✅ 良い
患者: 良いですね。施術時間短くなっても良いので、もっと早めの時間ありませんか？
Bot : （無反応）
患者: もしもし？
Bot : ただいま予約状況を確認いたしますので、少々お待ちいただけますでしょうか。
      担当の者が改めてご連絡いたしますね。                                        ← ❌ 手動退避
患者: 落ちた？
Bot : ご連絡ありがとうございます、お痛みは大丈夫でしょうか。
      現在システムがうまく動いていないようですので、この後の対応は担当の者に代わりますね。 ← ❌❌ 幻覚
```

**問題は2つ。**
1. **候補提示後の条件変更（交渉）に対応していない。** 「もっと早く」「短くてもいい」で手動退避に落ちた。
2. **LLMが存在しない事実を創作した。** システムは正常だったのに「システムがうまく動いていない」と患者に伝えた。「落ちた？」に対する「お痛みは大丈夫でしょうか」も文脈無視。

---

## 🔒 守る原則（変更禁止・指示書4から継続）

1. `create_reservation(..., reject_conflicts=True)` を**絶対に外さない**。
2. 対象者ゲート（`settings.line_autopilot_enabled` ＋ `patients.line_autopilot_enabled` ＋ 合言葉 `#autopilot-setup`）を**緩めない・広げない**。本番は今もスタッフ手打ち運用中で、自動化は登録済みの数名だけのテスト。
3. autopilot 対象外の患者・shadow 経路・旧分岐は**1バイトも変えない**。
4. テンプレで返すのは外部API障害時のみ。出たら `logger.error` ＋管理者通知。
5. PII（電話番号・生年月日）のLLM送信前マスキングを維持。
6. 既存テストを緑のまま。

---

# 第1部: LINE自動応答の修正

## タスクA【最優先】候補提示後の「交渉ループ」を実装する

**これが今回の主因。** 候補を出した後、患者が条件を変えてきたら、**手動退避せずに再検索して出し直す。**

### 対象の状態
`adjusting`（候補提示済み）/ `autopilot_booking_confirm`（枠確認中）/ `autopilot_change_confirm`（変更候補確認中）

### 認識すべき条件変更（LLMの `parsed_intent` と `constraints` を使う。キーワード一致で作り込まない）

| 患者の言い方 | 解釈 | 再検索の条件 |
|---|---|---|
| もっと早い時間／早めに | `earlier` | 提示済み候補の**最早時刻より前**で再検索 |
| もっと遅く／夕方以降で | `later` / `after:HH:MM` | 指定時刻以降で再検索 |
| 施術時間短くてもいい／短くても可 | `duration_flexible` | **メニューの最小時間まで**縮めて再検索（後述） |
| 別の日で／その日は無理 | `exclude_date` | その日を除外して再検索 |
| ◯曜以外で | `exclude_weekday` | 該当曜日を除外 |
| もっと早い日で／なるべく早く | `asap` | 直近から探す |
| ◯時までに終わりたい | `end_by:HH:MM` | 終了時刻から逆算 |

### 施術時間の短縮ルール（重要）
- 短縮の下限は **メニューの最小時間**（`_menu_duration_bounds` の min）。マッスルセラピーなら 35分。それより短くしない。
- 短縮した枠を提案するときは、**必ず所要時間を明示**する（例:「35分の枠でしたら11:15からご案内できます」）。患者が短くなったことを知らずに予約する状態を作らない。
- ⚠️ **料金には一切言及しない**（後述タスクE）。

### 実装方針
1. 候補提示時、提示した候補（日時・担当・所要時間）を `line_user_states.context_data` に保持する（再検索の基準として使う）。
2. 上記の状態で新規メッセージを受けたら:
   - まず `parsed_intent` を見る。**番号選択・明確な肯定・明確な否定でなければ「条件変更の可能性」として扱う。**
   - 条件を draft にマージし、**同じメニュー・同じ患者で再検索**して新しい候補を提示する。
   - 「該当なし」のときは、その旨を伝えて**次の選択肢を示す**（別日を提案する等）。黙って手動退避しない。
3. **手動退避は最後の手段**。以下のいずれかに限る:
   - 再検索しても候補が出せない状態が **3往復続いた**
   - `needs_human=true`（相談・クレーム・保険や料金の個別問い合わせ）
   - 例外発生
4. 無反応（返信なし）を絶対に作らない。**どのパスでも必ず1通返す。**

### 完了条件
実機ログの再現テストを追加し、以下が通ること:
- 候補3件を提示した状態で「施術時間短くなっても良いので、もっと早めの時間ありませんか？」を受信
  → **手動退避しない**／短縮した所要時間を明示した、より早い候補が提示される（空きがあれば）

---

## タスクB【最優先】幻覚の停止：原因・状態を創作させない

実機で「現在システムがうまく動いていないようですので」という**事実無根の説明**が患者に送られた。

### やること
1. `compose_reply` のプロンプトに**禁止事項**を追加する:
   - 「システムの状態・障害・エラー・処理状況について、**確定事実に無いことを書かない**」
   - 「患者の症状や体調について、**確定事実に無い推測を書かない**」（「お痛みは大丈夫ですか」を無関係な文脈で出さない）
   - 「担当者に代わる理由を**創作しない**。理由が確定事実に無ければ、理由に言及せず引き継ぎだけ伝える」
2. `handoff_to_human` の context に**理由を入れない**（入れるなら確定した理由のみ）。
   文面は「担当者からご連絡します」に留め、**原因の説明をさせない**。
3. 患者の直前メッセージへの共感は、**症状の言及が実際にあった場合のみ**。
   「落ちた？」「もしもし？」のような催促には、共感ではなく**素直な応答**（お待たせした旨＋現状）を返す。
4. 検証: 「もしもし？」「落ちた？」に対する応答に、`システム`／`障害`／`エラー`／`痛み` といった語が**文脈なく出ない**ことをテストで固定する。

---

## タスクC 「来週◯曜」の日付バグを直す（誤予約に直結）

### 現状の誤り
[backend/app/agents/line_parser.py:176](backend/app/agents/line_parser.py):

```python
days_ahead = (target_weekday - now.weekday()) % 7
if qualifier == "来週":
    days_ahead += 7          # ← 誤り
```

今日=木 2026-08-13 の場合:
- 今週(月-日) = 8/10〜8/16、来週(月-日) = 8/17〜8/23
- **「来週の火曜」= 8/18 が正解**
- 現実装は 直近火曜(8/18) + 7 = **8/25（1週間先へ誤る）**

Gemini稼働時はLLMが正しく 8/18 を返していたが、**API障害時にこのルールが使われると患者が1週間先に予約される。**

### やること
1. **ルール側を暦の週で計算し直す**:
   - 週の起点は月曜。`this_monday = today - timedelta(days=today.weekday())`
   - 「来週◯曜」= `this_monday + 7日 + 曜日index`
   - 「今週◯曜」= `this_monday + 曜日index`（既に過去なら、その旨を扱えるようにする／少なくとも過去日を予約希望日として返さない）
   - 「次の◯曜」「◯曜（単独）」= 直近の未来のその曜日（今日と同じ曜日なら7日後）＝現行踏襲
2. **LLMプロンプトにも定義を明記する**（現在プロンプトに「来週」の定義が**書かれておらず**、LLMの推測に依存している）:
   ```
   「来週◯曜」＝ 今日を含む週の次の週（月曜始まり）の◯曜日。
   「今週◯曜」＝ 今日を含む週（月曜始まり）の◯曜日。
   「次の◯曜」「◯曜」＝ 直近の未来のその曜日（今日が同じ曜日なら7日後）。
   ```
3. **評価コーパスの誤りを修正する**: [backend/tests/data/line_parse_corpus.jsonl](backend/tests/data/line_parse_corpus.jsonl) の `rel-01`
   （`来週の火曜、夕方以降で`）の `expect_date` を **`2026-08-25` → `2026-08-18`** に修正。
   ※ これは指示書作成者側の誤りで、LLMの出力の方が正しかった。**LLMを8/25に合わせる修正は絶対にしないこと。**
4. ルール単体テストを追加（木曜起点で「来週火曜=8/18」「今週金曜=8/14」「次の木曜=8/20」等）。

---

## タスクD constraints を空き枠検索へ実際に反映する

`constraints` は**抽出されてもログと文面生成に渡るだけ**で、候補生成に使われていない
（[line.py:775](backend/app/api/line.py) と [line.py:906](backend/app/api/line.py) 以外に参照が無い）。
`build_same_day_candidates` は `window_start_min` / `window_end_min` を受け取れるのに、橋渡しが無い。

### やること
最低限、以下を候補生成へ反映する:
- `window:HH:MM-HH:MM` → `window_start_min` / `window_end_min`（既存引数にそのまま渡せる）
- `after:HH:MM` → 開始時刻の下限
- `before:HH:MM` / `end_by:HH:MM` → 終了時刻の上限（`end_by` は所要時間から逆算）
- `exclude_weekday:xxx` / `exclude_date:YYYY-MM-DD` → 候補日から除外
- `asap` → 直近日から順に探索

タスクAの交渉ループもこの仕組みに乗せる。

---

## タスクE システム由来の事実には答える（料金は除外）

「予約以外は全部手動」だと受付として弱い。**システムが事実として持っているものだけ**答えられるようにする。

### 答えてよい（DBが正・メンテ不要）
- **営業日・休診日** … `business_hours` / `date_overrides`（`get_business_hours_for_date`）
- **施術者の出勤・休み** … `practitioner_schedules` / `practitioner_unavailable_time`
- **空き状況** … 既存の枠検索
- **メニュー名・所要時間・時間の選び方**（例:「10分単位でお時間を選べます」）… `menus`（`is_duration_variable` / `duration_minutes` / `max_duration_minutes`）

### 答えてはいけない
- **料金・費用・保険適用の可否** … `menus.price` / `menu_price_tiers` は**未入力のメニューがあり、10分単位で変動するものもある**ため、
  システムの値を根拠に回答すると誤案内になる。
  → **プロンプトで明示的に禁止**し、「料金はスタッフからご案内します」と伝えて手動へ渡す（`needs_human`）。
- 持ち物・駐車場・アクセス・施術内容の詳細（＝DBに無いもの）→ 手動へ。

### 実装方針
- 「システムに問い合わせて事実を取得する関数」を1つ用意し、`intent == "question"` かつ上記カテゴリのときに呼ぶ。
  取得した事実を context に入れて `compose_reply` に自然文を書かせる（**LLMに知識を持たせない。必ずDBの値から答えさせる**）。
- カテゴリ外・データ無しなら手動へ。**推測で答えさせない。**

---

# 第2部: HotPepper未同期618件の掃除

## タスクF-1 なぜ過去の予約が一覧に出るのか調査する

**前提（調査前に把握しておくこと）**: `/hotpepper/pending-sync` には既に日付の絞り込みが入っている
（[backend/app/api/hotpepper.py:55](backend/app/api/hotpepper.py) `Reservation.start_time >= now` / `<= horizon`）。
`start_time` は `DateTime(timezone=True)`。それでも本番の一覧に 2026/4/9 等の**過去**が出て 618 件になっている。

以下を順に確認し、**原因を特定してから**修正すること:
1. 本番Renderで動いているコミットが、この日付フィルタを含んでいるか（デプロイ漏れの確認）。
2. フロントエンド [frontend/src/components/HotPepperSync.tsx](frontend/src/components/HotPepperSync.tsx) が実際に叩いているエンドポイントとレスポンス。
   別経路・キャッシュ・古いビルドの可能性。
3. 表示している日付が `start_time` なのか、別のフィールド（`created_at` / シリーズの初回日など）なのか。
4. `now_jst()` の値と、本番DBの `start_time` の型・タイムゾーン（本番で `timestamptz` か）。
5. `status` が `CONFIRMED/PENDING/HOLD` のまま残っている過去予約の実件数（日付帯別の内訳）。

**調査結果を記録すること**（原因が特定できないまま次へ進まない）。

## タスクF-2 過去分を一括で「同期済み」にする（オーナー承認済み）

方針は決定済み: **過去の未同期予約は一括で `hotpepper_synced = true` にして帳消しにする。**

⚠️ **本番データを書き換える破壊的操作。過去に無断デプロイで本番事故を起こしている案件なので、以下を厳守すること。**

1. **マイグレーションで勝手に実行しない。** 管理画面から実行する**管理者専用エンドポイント**として作る。
2. **必ず2段階**にする:
   - **ドライラン**: 対象件数と日付範囲の内訳だけ返す（何も変更しない）
   - **実行**: 明示的な確認を経てから更新
3. 対象条件を厳密に:
   - `hotpepper_synced == False`
   - `channel != "HOTPEPPER"`
   - `start_time < 今日の0:00（JST）` … **過去のみ**。未来は絶対に触らない
4. **監査ログを残す**（`audit_logs`）。誰が・いつ・何件を同期済みにしたか。
5. **UIに確認ダイアログ**（件数を表示して「◯件を同期済みにします。よろしいですか？」）。
6. 実行後、一覧と件数が即座に更新されること。

## タスクF-3 再発防止

- 一覧・件数・RPAキューの**すべての経路**で「過去は出さない」ことをテストで固定する
  （過去・今日・未来・90日超のデータを用意し、過去と90日超が除外されることを検証）。
- タスクF-1で判明した原因に対する再発防止（デプロイ漏れならデプロイ手順、表示バグならフロント修正）。

---

# 第3部: HP側FAQ・料金表の連携（**調査のみ。実装しない**）

オーナー判断: **今回は調査だけ。実装は次回判断する。**

以下を調べて**選択肢と判断材料をレポートにまとめる**こと（コードは書かない）:

1. ホームページ（coco整骨院サイト）と本予約システムは、**同じPostgreSQLインスタンス／同じデータベース**を使っているか。
   スキーマは分かれているか。予約システム側から直接読めるのか。
2. HP側の FAQ と 料金表が、**どのテーブル／どの形式**で保存されているか（CMS化されている想定）。
3. 予約システムから参照する現実的な方式の比較:
   - (a) HP側に公開APIを作って予約システムから呼ぶ
   - (b) 共有DBを直接読む（スキーマ跨ぎ）
   - (c) 定期同期でコピーを持つ
   それぞれの**メンテナンスコスト・結合度・障害時の影響**を1〜2行で評価。
4. 料金表については、**HP側の料金データが完全か**（空欄・「10分毎に1000円」のような可変表記がどう保存されているか）を確認する。
   → 可変・未入力があるなら、LINEでの自動回答には**使えない**と結論づけてよい。

**レポートのみ。DBスキーマの変更・HP側リポジトリの変更は行わない。**

---

# 検証（必須）

1. **交渉ループ再現テスト**: 候補提示後の「施術時間短くなっても良いので、もっと早めの時間ありませんか？」で
   手動退避せず、所要時間を明示した早い候補が返ること。
2. **幻覚禁止テスト**: 「もしもし？」「落ちた？」への応答に `システム`／`障害`／`エラー`／`痛み` が文脈なく現れないこと。
3. **来週テスト**: 木曜起点で「来週の火曜」が **8/18**（ルール側・LLM側の両方）。コーパス `rel-01` 修正済み。
4. **料金拒否テスト**: 「マッスルセラピーいくら？」で金額を答えず手動へ渡すこと。
5. **HotPepperテスト**: 過去・今日・未来・90日超のデータで、一覧と件数から過去と90日超が除外されること。
   一括同期のドライランが正しい件数を返し、**未来を1件も含まない**こと。
6. **評価 & 回帰**:
   ```bash
   python -m scripts.eval_line_parser --sleep 1.5
   python -m pytest tests/test_shadow_mode.py tests/test_line_ai_secretary.py -q
   ```
   制約再現（constraints）がタスクD後に**現状51.1%から明確に向上**していること。数値を記録。
7. **安全確認**: `reject_conflicts=True` 維持／対象者ゲート不変／非autopilot経路 無変更。

---

# 完了条件

- [ ] 候補提示後の条件変更で手動退避しない。無反応が発生しない。
- [ ] 患者への返信に、事実に無いシステム状態・症状の推測が出ない。
- [ ] 「来週の火曜」がルール・LLM・コーパスすべてで 8/18。
- [ ] constraints が候補生成に反映され、評価値が向上。
- [ ] 営業日・休診日・施術者の休み・空き・メニュー所要時間に自動回答できる。**料金は答えない。**
- [ ] HotPepper未同期一覧から過去が消え、618件の過去分を管理画面から安全に一括処理できる（ドライラン＋監査ログ付き）。
- [ ] HP連携は**調査レポートのみ**（実装なし）。
- [ ] 検証1〜7すべて通過。🔒原則1〜6 無傷。

---

## Copilotへの一言（コピペ用）

```
リポジトリ直下の INSTRUCTIONS_20260818_NEGOTIATION_AND_CLEANUP.md を読んで実装して。
指示書4のLLM-ONLY方針は継続（LLMが読み、LLMが書き、そのまま送る）。

優先順: タスクA（交渉ループ）→ B（幻覚停止）→ C（来週バグ）→ D（constraints橋渡し）→ E（システム由来の事実に回答・料金は除外）→ 第2部F（HotPepper調査＋過去分の安全な一括同期）→ 第3部（HP連携は調査レポートのみ）。

特に注意:
- タスクC: 「来週の火曜」はLLMの 8/18 が正しい。コーパスの 8/25 が誤り。LLMを8/25に合わせる修正は絶対にしない。
- タスクF-2: 本番データの破壊的更新。マイグレーションで勝手に流さず、管理者エンドポイント＋ドライラン＋確認ダイアログ＋監査ログにすること。未来の予約は絶対に触らない。
- 第3部は調査レポートのみ。コードを書かない。

🔒原則（reject_conflicts=True維持／対象者ゲート不変／非autopilot経路は変更しない／テンプレはAPI障害時のみ）は厳守。
検証1〜7を必ず実行して結果を記録すること。
```
